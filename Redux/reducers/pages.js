const initialState = {};
const pagesReducer = (state = initialState, action) => {
  const ap = action.payload;
  switch (action.type) {
    default:
      return state;
  }
};

export default pagesReducer;
